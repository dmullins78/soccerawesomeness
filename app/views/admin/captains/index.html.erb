<style>
  textarea {
    width:500px;
  }
</style>
`
<%= form_for :captains, :html => { :class => "well" } do |f| %>

  <fieldset>
    <%= f.label "Captains/Teams" %>
    <%= f.text_area :captains, size: "80x30"%>

    <br/>

    <div class="form-actions">
      <%= f.submit 'Save', :class => 'btn btn-primary' %>
      <input type="button" class="btn" value="Cancel" onclick="location='/teams'"/> 
    </div>

  </fieldset>

<% end %>
